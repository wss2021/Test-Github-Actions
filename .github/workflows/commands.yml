on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    - run: |
        dep=false
        if [[ dep ]]; then
          echo "::warning::Deprecated Function Found!"
        fi
