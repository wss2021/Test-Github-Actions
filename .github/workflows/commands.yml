on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
    - name: Set API Key
      run: echo "API_Key=12345"
    - run: |
        dep= false
        if [[ API_Key==12345 ]]; then
          echo "::warning::Deprecated Function Found!"
        fi
